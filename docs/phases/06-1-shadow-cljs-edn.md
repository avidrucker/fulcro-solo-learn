# Phase 6.1 — shadow-cljs.edn + browser target

**Status:** ✅ Complete
**Parent:** [Phase 6 — shadow-cljs + browser app (no real backend)](06-shadow-cljs.md)

Add `shadow-cljs.edn`, declare a browser target, get a trivial "Hello" JS bundle building. (`build` commit; deps come from deps.edn via the new `:cljs` alias; `npm install` bootstraps shadow-cljs + react/react-dom.)
